# Citizen sample pool

`sample_pool.json` is an offline, checked-in flavor pool prepared from the synthetic
`nvidia/Nemotron-Personas-El-Salvador` dataset (CC BY 4.0). It is not a demographic truth source.
The game uses it for profile variety only; cohort formulas remain authoritative for economy, risk,
corruption and political state.
