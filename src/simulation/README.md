# Simulation

Headless city state and domain systems. It must never import from `rendering/` or `ui/`.
Population begins as aggregate cohorts; detailed agents are reserved for later event-driven use.
